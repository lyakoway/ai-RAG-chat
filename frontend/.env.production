# Публичные ID счётчиков (видны в HTML/JS клиента — это не секреты).
# Vite подхватывает файл при `npm run build` / Docker-сборке на HF Spaces,
# где Variables из Settings на этапе build обычно недоступны.

VITE_YANDEX_METRIKA_ID=111388823
VITE_GA4_MEASUREMENT_ID=G-T6FT8QW8Y7
