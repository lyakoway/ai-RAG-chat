# Аналитика (публичные ID счётчиков — попадают в клиентский бандл).
# Локально: скопируйте в frontend/.env.local и заполните.
# Прод (HF / Docker): значения заданы в frontend/.env.production
# (Variables в HF Settings на этапе Vite-build обычно не видны).

VITE_YANDEX_METRIKA_ID=
VITE_GA4_MEASUREMENT_ID=
