# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Persistence ---
SQLAlchemy==2.0.36

# --- Vector store & embeddings ---
chromadb==0.5.23
fastembed==0.5.1          # local ONNX embeddings + cross-encoder reranker
rank-bm25==0.2.2          # lexical search for hybrid retrieval (pure python)

# --- Document parsing ---
pypdf==5.1.0              # PDF text + page numbers
python-docx==1.1.2        # Word .docx
openpyxl==3.1.5           # Excel .xlsx

# --- LLM providers (optional at runtime) ---
openai==1.59.6
anthropic==0.42.0
httpx==0.28.1             # used for Ollama HTTP calls

# --- Misc ---
tiktoken==0.8.0           # token-aware chunking
